global:
  resolve_timeout: 5m

route:
  receiver: "telegram-bot"
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 30m

receivers:
  - name: "telegram-bot"
    webhook_configs:
      - url: "http://localhost:5000/alert"
        send_resolved: false
